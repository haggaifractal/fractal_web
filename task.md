# Tasks

- [ ] Move CSS animations (`marquee-up`, `marquee-down`) from `ClientsMarquee.astro` to global `Layout.astro`.
- [ ] Add `ctaBoxText` and `ctaBoxLink` to `he.json` and `en.json`.
- [ ] Modify `CustomersTemplate.astro` to hide the Grid on mobile.
- [ ] Add the dual-scroll marquee layout to `CustomersTemplate.astro` for mobile only.
- [ ] Inject the CTA box with an icon in the first iteration of the right column, without duplicating it in the second iteration.
- [ ] Verify functionality.
